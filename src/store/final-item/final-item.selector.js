export const selectSizeSelectedPrice = (state) =>
  state.finalItem.sizeSelectedPrice;

export const selectChosenSize = (state) => state.finalItem.chosenSize;

export const selectTypeSelectedPrice = (state) =>
  state.finalItem.typeSelectedPrice;

export const selectChosenType = (state) => state.finalItem.chosenType;

export const selectSaltAndVinegar = (state) => state.finalItem.saltAndVinegar;

export const selectPie = (state) => state.finalItem.pie;

export const selectGratedCheesePrice = (state) =>
  state.finalItem.gratedCheesePrice;

export const selectDonerMeatPrice = (state) => state.finalItem.donerMeatPrice;

export const selectCheeseSliceSelected = (state) =>
  state.finalItem.cheeseSliceSelected;

export const selectSaladSelected = (state) => state.finalItem.saladSelected;

export const selectSaucesSelected = (state) => state.finalItem.saucesSelected;

export const selectCan = (state) => state.finalItem.can;

export const selectBottles = (state) => state.finalItem.bottles;

export const selectSingleCondiment = (state) => state.finalItem.singleCondiment;

export const selectTwoCondiments = (state) => state.finalItem.twoCondiments;

export const selectConeOfChipsSauce = (state) =>
  state.finalItem.coneOfChipsSauce;

export const selectOneMeat = (state) => state.finalItem.oneMeat;

export const selectTwoMeats = (state) => state.finalItem.twoMeats;

export const selectSpecialInstructions = (state) =>
  state.finalItem.specialInstructions;

export const selectQuantity = (state) => state.finalItem.quantity;
