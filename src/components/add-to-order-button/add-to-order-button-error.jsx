import { Text, Span } from "./add-to-order-button.styles";
const AddToOrderButtonError = () => {
  return (
    <Text>
      the add to order button will show when all <Span>Required</Span> fields
      have been selected.
    </Text>
  );
};

export default AddToOrderButtonError;
