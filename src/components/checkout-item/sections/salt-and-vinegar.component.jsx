const SaltAndVinegar = ({ saltAndVinegar }) => (
  <>
    {saltAndVinegar && (
      <>
        <span>salt & vinegar?</span>
        <p>{saltAndVinegar}</p>
      </>
    )}
  </>
);

export default SaltAndVinegar;
