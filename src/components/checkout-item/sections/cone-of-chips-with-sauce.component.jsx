const coneOfChipsWithSauce = ({ coneOfChipsSauce }) => (
  <>
    {coneOfChipsSauce && (
      <>
        <span>sauce:</span>
        <p>{coneOfChipsSauce}</p>
      </>
    )}
  </>
);

export default coneOfChipsWithSauce;
